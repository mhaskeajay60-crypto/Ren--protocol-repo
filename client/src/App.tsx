/**
 * THE EMBER ARCHIVE — The application is intentionally delivered as a standalone
 * browser document at client/index.html so it can remain local-first and dependency-free.
 * This compatibility module remains unused by that document.
 */
export default function App() {
  return null;
}
